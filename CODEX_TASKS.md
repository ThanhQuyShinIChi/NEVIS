# CODEX Tasks

- [x] Task 2: Save and load `Node.z` in `.nevis` project files with safe legacy defaults.
- [x] Task 3: Save and load `Edge.start_z`, `Edge.end_z`, and `Edge.slope` with safe legacy defaults.
- [x] Task 4: Validate `Node.z` input in the UI before updating the model.
